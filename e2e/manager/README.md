It's e2e test for spaship manager

How to test:

$ npm install
$ npm test -- -e {hostname} -u {login_username} -p {login_passw} -a {application_path}

Check the test result in /cypress/reports
Check the test vedio in /cypress/vedio
